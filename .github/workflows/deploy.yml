name: Frontend CI/CD Pipeline
on:
  push:
    branches:
      - main
      - develop
      - prod
  pull_request:
    branches:
      - main
      - prod

jobs:
  # 🧪 JOB 1: Tests and Quality code
  test-and-quality:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          # Important: permet de push les changements
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      # ✨ NOUVEAU: Auto-format avec Prettier
      - name: 🎨 Auto-format code with Prettier
        id: prettier-format
        run: |
          echo "🔧 Application du formatage Prettier..."
          npm run format
          
          # Vérifier s'il y a des changements
          if git diff --quiet; then
            echo "✅ Aucun changement de formatage nécessaire"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "📝 Fichiers formatés automatiquement"
            echo "changed=true" >> $GITHUB_OUTPUT
            git diff --name-only
          fi

      - name: 💾 Commit formatting changes
        if: steps.prettier-format.outputs.changed == 'true' && github.event_name == 'push'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "🎨 Auto-format code with Prettier [skip ci]"
          git push
          echo "✅ Changements de formatage committé"

      - name: 🔍 Run ESLint
        run: npm run lint
        continue-on-error: false

      - name: 🧪 Run unit tests
        run: npm run test:run
        env:
          CI: true

      - name: 📊 Generate coverage report
        run: npm run test:coverage
        continue-on-error: true

      - name: 🔒 Security audit
        run: npm audit --audit-level moderate
        continue-on-error: true

      - name: 🏗️ Test build
        run: npm run build
        env:
          VITE_RECAPTCHA_SITE_KEY: ${{ secrets.RECAPTCHA_SITE_KEY }}

      - name: ✅ Tests Summary
        run: |
          echo "🎉 Tests réussis: Tous les tests passent!"
          echo "✅ Build de test réussi"
          echo "✅ ESLint validé"
          if [ "${{ steps.prettier-format.outputs.changed }}" == "true" ]; then
            echo "🎨 Code automatiquement formaté"
          fi

  # 🏗️ JOB 2: Build for deployment
  build-for-deploy:
    needs: test-and-quality
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/prod' && github.event_name == 'push'
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Debug environment
        run: |
          echo "Node version: $(node --version)"
          echo "NPM version: $(npm --version)"
          echo "Current branch: ${{ github.ref }}"
          echo "Event: ${{ github.event_name }}"

      - name: 🏗️ Build project
        run: npm run build
        env:
          VITE_RECAPTCHA_SITE_KEY: ${{ secrets.RECAPTCHA_SITE_KEY }}
          NODE_ENV: production

      - name: 🔍 Verify build output
        run: |
          echo "📂 Build directory contents:"
          ls -la dist/

          echo "📊 Build size analysis:"
          du -sh dist/
          find dist/ -name "*.js" -exec du -h {} \; | sort -hr | head -5
          find dist/ -name "*.css" -exec du -h {} \; | sort -hr | head -3

          # Vérifications critiques
          test -f dist/index.html || (echo "❌ index.html missing" && exit 1)
          test -d dist/assets || (echo "❌ assets directory missing" && exit 1)

          echo "✅ Build verification successful"

      - name: 📦 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-files-${{ github.sha }}
          path: dist/
          retention-days: 1
          compression-level: 6
          if-no-files-found: error

  # 🚀 JOB 3: Deployment with optimized caching and MIME types
  deploy:
    needs: build-for-deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/prod' && github.event_name == 'push'
    steps:
      - name: 📥 Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-files-${{ github.sha }}
          path: dist/

      - name: 🔍 Verify downloaded artifacts
        run: |
          echo "📂 Downloaded artifacts:"
          ls -la dist/

          echo "📊 Assets breakdown:"
          if [ -d "dist/assets" ]; then
            echo "Assets directory contents:"
            ls -la dist/assets/
            ls -la dist/assets/js/ 2>/dev/null || echo "No JS folder"
          else
            echo "❌ No assets directory found!"
            exit 1
          fi

      - name: 🚀 Deploy all assets to S3 first
        run: |
          echo "📦 Deploying all assets to S3..."
          aws s3 sync dist/assets/ s3://${{ secrets.S3_BUCKET_NAME }}/assets/ \
            --cache-control "public, max-age=31536000, immutable" \
            --delete \
            --region eu-west-3 \
            --exclude "*.map"
          echo "✅ Assets deployed"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 🔧 Fix MIME types for JavaScript files
        run: |
          echo "🔧 Fixing MIME types for JavaScript files..."
          aws s3 cp s3://${{ secrets.S3_BUCKET_NAME }}/assets/js/ s3://${{ secrets.S3_BUCKET_NAME }}/assets/js/ \
            --recursive \
            --metadata-directive REPLACE \
            --content-type "application/javascript" \
            --cache-control "public, max-age=31536000, immutable" \
            --region eu-west-3
          echo "✅ JavaScript MIME types fixed"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 🔧 Fix MIME types for CSS files
        run: |
          echo "🔧 Fixing MIME types for CSS files..."
          aws s3 cp s3://${{ secrets.S3_BUCKET_NAME }}/assets/ s3://${{ secrets.S3_BUCKET_NAME }}/assets/ \
            --recursive \
            --metadata-directive REPLACE \
            --content-type "text/css" \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*" \
            --include "*.css" \
            --region eu-west-3
          echo "✅ CSS MIME types fixed"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 📄 Deploy HTML and other files with short cache
        run: |
          echo "📄 Deploying HTML and other files with 5 minutes cache..."
          aws s3 sync dist/ s3://${{ secrets.S3_BUCKET_NAME }}/ \
            --exclude "assets/*" \
            --exclude "*.map" \
            --content-type "text/html" \
            --cache-control "public, max-age=300" \
            --delete \
            --region eu-west-3
          echo "✅ HTML and other files deployed"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 🔍 Verify S3 deployment and MIME types
        run: |
          echo "🔍 Verifying deployment..."
          aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }}/ --recursive --human-readable --summarize | tail -10

          echo "🔍 Checking specific files existence..."
          # Vérifier l'existence des fichiers clés
          if aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }}/assets/index-3mRPG_9E.css; then
            echo "✅ CSS file exists"
            aws s3api head-object --bucket ${{ secrets.S3_BUCKET_NAME }} --key assets/index-3mRPG_9E.css | grep ContentType || echo "No ContentType found"
          else
            echo "❌ CSS file NOT found on S3"
          fi

          if aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }}/assets/js/index-D5OH-pp8.js; then
            echo "✅ JS file exists"
            aws s3api head-object --bucket ${{ secrets.S3_BUCKET_NAME }} --key assets/js/index-D5OH-pp8.js | grep ContentType || echo "No ContentType found"
          else
            echo "❌ JS file NOT found on S3"
          fi

          echo "📋 Listing all JS files in bucket:"
          aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }}/assets/js/ || echo "No JS directory found"

          echo "📋 Listing all CSS files in bucket:"
          aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }}/assets/ | grep "\.css" || echo "No CSS files found"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: ♻️ Invalidate CloudFront cache
        uses: chetan/invalidate-cloudfront-action@v2
        env:
          DISTRIBUTION: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          PATHS: '/*'
          AWS_REGION: eu-west-3
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: ✅ Deployment success
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🌐 Application deployed to S3 with optimized caching and correct MIME types:"
          echo "   📦 JavaScript files: application/javascript (1 year cache)"
          echo "   🎨 CSS files: text/css (1 year cache)"
          echo "   🖼️ Assets (fonts/images): appropriate MIME types (1 year cache)"
          echo "   📄 HTML files: text/html (5 minutes cache)"
          echo "♻️ CloudFront cache invalidated"

  # 🔄 JOB 4: Post-deployment verification
  verify-deployment:
    needs: deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/prod' && github.event_name == 'push'
    steps:
      - name: 🏥 Health check
        run: |
          echo "🔍 Performing post-deployment checks..."

          # Test basique de connectivité
          if curl -f -s -o /dev/null https://wonderbook.net; then
            echo "✅ Site accessible"
          else
            echo "❌ Site not accessible"
            exit 1
          fi

      - name: 🔍 Verify MIME types in production
        run: |
          echo "🔍 Testing MIME types in production..."

          # Test d'un fichier CSS récent (pas forcément le nom exact)
          CSS_FILE=$(curl -s https://wonderbook.net/ | grep -o 'assets/index-[^"]*\.css' | head -1)
          if [ -n "$CSS_FILE" ]; then
            CSS_HEADER=$(curl -I -s "https://wonderbook.net/$CSS_FILE" | grep -i content-type || echo "CSS not found")
            echo "📄 CSS Content-Type: $CSS_HEADER"
          else
            echo "📄 CSS file not found in HTML"
          fi

          # Test d'un fichier JS récent (pas forcément le nom exact)
          JS_FILE=$(curl -s https://wonderbook.net/ | grep -o 'assets/js/index-[^"]*\.js' | head -1)
          if [ -n "$JS_FILE" ]; then
            JS_HEADER=$(curl -I -s "https://wonderbook.net/$JS_FILE" | grep -i content-type || echo "JS not found")
            echo "📦 JS Content-Type: $JS_HEADER"
          else
            echo "📦 JS file not found in HTML"
          fi

          # Test de l'index.html
          HTML_HEADER=$(curl -I -s https://wonderbook.net/ | grep -i content-type || echo "HTML not found")
          echo "🏠 HTML Content-Type: $HTML_HEADER"

          echo "✅ MIME type verification completed!"

      - name: 🔍 Verify cache headers
        run: |
          echo "🔍 Testing cache headers..."

          # Test d'un asset JS
          ASSET_RESPONSE=$(curl -I -s https://wonderbook.net/assets/js/index-D5OH-pp8.js | head -n 20)
          echo "📦 Asset headers sample:"
          echo "$ASSET_RESPONSE"

          # Test de l'index.html
          HTML_RESPONSE=$(curl -I -s https://wonderbook.net/ | head -n 20)
          echo "📄 HTML headers:"
          echo "$HTML_RESPONSE"

          echo "✅ Post-deployment verification completed!"